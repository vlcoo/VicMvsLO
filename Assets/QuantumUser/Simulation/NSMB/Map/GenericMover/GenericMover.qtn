component GenericMover {
	list<PathNode> Path;
	LoopingMode LoopingMode;
	
	FP StartOffset;
	bool DurationIsSpeedInstead;
}

struct PathNode {
    FPVector2 Position;
    FP TravelDuration;
    bool EaseIn;
    bool EaseOut;
}

enum LoopingMode {
    Clamp,
    Loop,
    PingPong,
}
