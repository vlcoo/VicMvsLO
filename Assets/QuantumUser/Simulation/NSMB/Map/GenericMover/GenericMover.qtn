component GenericMover {
	list<PathNode> Path;
	LoopingMode LoopingMode;
	
	FP StartOffset;
}

struct PathNode {
    FPVector2 Position;
    FP TravelDuration;
    bool EaseIn;
    bool EaseOut;
}

enum LoopingMode {
    Clamp,
    Loop,
    PingPong,
}
