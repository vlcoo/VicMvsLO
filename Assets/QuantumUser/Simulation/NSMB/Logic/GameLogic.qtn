#pragma max_players 10

global {
	GameRules Rules;	
	GameState GameState;
	int StartFrame;
	int TotalGamesPlayed;
	ushort GameStartFrames;
	ushort PlayerLoadFrames;

	[AllocateOnComponentAdded] dictionary<PlayerRef, EntityRef> PlayerDatas;
	array<PlayerInformation>[10] PlayerInfo;
	byte RealPlayers;
	byte TotalMarios;

	int WinningTeam;
	bool HasWinner;
}

struct PlayerInformation {
	PlayerRef PlayerRef;
	QString<48> Nickname;
	QString<16> NicknameColor;
	byte Team;
	byte Character;
	bool Disconnected;
	bool Disqualified;
}

struct GameRules {
	asset_ref<Map> Stage;

	int StarsToWin;
	int CoinsForPowerup;
	int Lives;
	int TimerSeconds;
	bool TeamsEnabled;
	bool CustomPowerupsEnabled;
	bool DrawOnTimeUp;
	int Laps;
	list<MatchConditionerTrigger> Triggers;
}

enum GameState : Byte {
    PreGameRoom,
    WaitingForPlayers,
    Starting,
    Playing,
    Ended
}

struct MatchConditionerTrigger {
    TriggerCondition Condition;
    QString<64> ConditionParameter;
    TriggerTarget ConditionTarget;
    TriggerAction Action;
    QString<64> ActionParameter;
    TriggerTarget ActionTarget;
    TriggerConstraint Constraint;
    QString<64> ConstraintParameter;
    TriggerTarget ConstraintTarget;
}

enum TriggerCondition {
    GotStar,
    GotCoin,
    Spawned,
    MatchStarted,
//    GotCheckpoint,
    GotPowerup,
    LostPowerup,
//    HitBlock,
    Stunned,
    Frozen,
//    Thawed,
//    StunnedSomeone,
//    SteppedOnEnemy,
//    TriggeredPowerup,
    Died,
    Jumped,
//    LookedXDirection,   // any, up, right, down, left
//    Ran,
    ReachedCoinLimit,
    Disqualified,
//    XSecondRemaining,   // 60, 10
//    EveryXSecond, // 1, 5, 10, 15, 30, 60
//    SongBahd,
    ReachedZeroCoins,
    ReachedZeroStars,
//    GotStarcoin,
//    EnteredPipe,
//    GrabbedSomething,
//    FinishedLap,
//    TouchedGround,
//    StoppedMoving,
//    KilledSomeone,
//    HarmedSomeone,
}

enum TriggerAction {
    GiveStar,
    GiveCoin,
    Kill,
    RemoveStar,
    RemoveCoin,
    GiveXPowerup,   // random, mushroom, fire, ice, propeller, shell, hammer, starman, mega, mini
//    GiveLife,
    Win,
    DrawMatch,
    Disqualify,
    Stun,    // bump, knockback, hardknockback
//    Dive,
//    Launch,
    Freeze,
    Harm,
    SpawnXPowerup,   // random, mushroom, fire, ice, propeller, shell, hammer, starman, mega, mini
//    SpawnXEnemy,    // random, goomba, Gkoopa, Rkoopa, Bkoopa, bulletbill, boo, spiny, bobomb
    RespawnLevel,
//    ExplodeLevel,
//    RandomTeleport,
    RemoveReserve,
    GiveXReserve,   // random, mushroom, fire, ice, propeller, shell, hammer, starman, mega, mini
    GiveIFrames,
    SpawnLooseCoin,
    SpawnLooseStar,
    ZeroCoins,
    ZeroStars,
    MaxCoins,
//    SpawnStar,
//    BecomeXTeam,    // random, A, B, C, D, E
}

enum TriggerConstraint {
    Always,
    Randomly,
    IsMoving,
    IsStationary,
    IsAirborne,
    IsGrounded,
    IsXPowerup,   // random, mushroom, fire, ice, propeller, shell, hammer, starman, mega, mini
    IsNotXPowerup,   // random, mushroom, fire, ice, propeller, shell, hammer, starman, mega, mini
    HasXCoins,  // any X in valid range vvv
    HasXStars,
    HasXLives,
    HasLessThanXCoins,
    HasLessThanXStars,
    HasLessThanXLives,
    HasMoreThanXCoins,
    HasMoreThanXStars,
    HasMoreThanXLives,
    TimerIsLessThanX,
    TimerIsMoreThanX,
    StarsExist,
    StarsNotExist,
    EnemiesExist,
    EnemiesNotExist,
    CoinsExist,
    CoinsNotExist,
    XPlayersRemaining,
    LessThanXPlayersRemaining,
    MoreThanXPlayersRemaining,
}

enum TriggerTarget {
    Everyone,
    Any,
    OneRandom,
    Randoms,
    Conditioner,
    ConditionerTeam,
    NonConditioner,
    NonConditionerTeam,
    Actioner,
    ActionerTeam,
    NonActioner,
    NonActionerTeam,
    Host,
    NonHost,
//    FirstPlace,
//    NonFirstPlace,
//    LastPlace,
//    NonLastPlace,
    TeamA,
    NonTeamA,
    TeamB,
    NonTeamB,
    TeamC,
    NonTeamC,
    TeamD,
    NonTeamD,
    TeamE,
    NonTeamE,
}

enum TriggerParameter {
    Random,
    Mushroom,
    Fire,
    Ice,
    Propeller,
    Shell,
    Hammer,
    Starman,
    Mega,
    Mini,
    Goomba,
    GreenKoopa,
    RedKoopa,
    BlueKoopa,
    BulletBill,
    Boo,
    Spiny,
    Bobomb,
    Buzzy,
    A,
    B,
    C,
    D,
    E,
    Bump,
    Knockback,
    HardKnockback,
    Force,
}

signal OnLoadingComplete();
signal OnGameStarting();
signal OnGameEnding(int winningTeam, bool hasWinner);
signal OnReturnToRoom();
signal OnTimerSecondsRemaining(int secondsRemaining);


synced event GameStateChanged {
	Frame Frame;
	GameState NewState;
}

synced event PlayerAdded {
	Frame Frame;
	PlayerRef Player;
}

synced event PlayerRemoved {
	Frame Frame;
	PlayerRef Player;
}

event PlayerDataChanged {
	nothashed Frame Frame;
	PlayerRef Player;
}

event StartingCountdownChanged {
	nothashed Frame Frame;
	bool IsGameStarting;
}

event CountdownTick {
	nothashed Frame Frame;
	int SecondsRemaining;
}

synced event HostChanged {
	Frame Frame;
	PlayerRef NewHost;
}

synced event RulesChanged {
	Frame Frame;
	bool LevelChanged;
}

synced event TriggersChanged {
    Frame Frame;
}

synced event PlayerLoaded {
	Frame Frame;
	PlayerRef Player;
}

synced event RecordingStarted {
	Frame Frame;
}

synced event GameStarted {
	Frame Frame;
}

synced event GameEnded {
	Frame Frame;
	int WinningTeam;
	bool HasWinner;
}

event StartGameEndFade {
	nothashed Frame Frame;
}